import 'package:flutter/material.dart';
import 'package:restaurant_saas/widgets/shimmer.dart';

Widget shimmerCoupon() => Column(
    children: [couponsList(), couponsList(), couponsList(), couponsList()]);
